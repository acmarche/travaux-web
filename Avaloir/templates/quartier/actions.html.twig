{% if is_granted('ROLE_TRAVAUX_AVALOIR') %}

    <a href="{{ path('quartier') }}" class="btn btn-primary" role="button"><i
                class="ti ti-arrow-left"></i>
        Retour à la liste</a>

    <a href="{{ path('quartierrue_new', { 'id': quartier.id }) }}" class="btn btn-primary"
       role="button"><i class="ti ti-list"></i> Ses rues</a>

    <a href="{{ path('nettoyage_quartier_new', { 'id': quartier.id }) }}" class="btn btn-primary"
       role="button"><i class="ti ti-calendar"></i> Ajouter une date de nettoyage</a>

    <a href="{{ path('quartier_edit', { 'id': quartier.id }) }}" class="btn btn-primary" role="button"><i
                class="ti ti-pencil"></i>
        Editer</a>

    <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
        <i class="ti ti-trash"></i> Supprimer
    </button>
    <br/>

    <br/>
{% endif %}