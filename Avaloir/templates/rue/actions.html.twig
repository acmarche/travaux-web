{% if is_granted('ROLE_TRAVAUX_AVALOIR') %}

    <a href="{{ path('rue') }}" class="btn btn-primary" role="button"><i
                class="ti ti-arrow-left"></i>
        Retour à la liste</a>

    <a href="{{ path('rue_edit', { 'id': rue.id }) }}" class="btn btn-primary" role="button"><i
                class="ti ti-pencil"></i>
        Editer</a>

    <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
        <i class="ti ti-trash"></i> Supprimer
    </button>
    <br/><br/>
{% endif %}
