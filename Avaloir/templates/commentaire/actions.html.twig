{% if is_granted('ROLE_TRAVAUX_AVALOIR') %}

    <a href="{{ path('avaloir_show',{'id':avaloir.id}) }}" class="btn btn-primary" role="button"><i
                class="ti ti-arrow-left"></i>
        Retour à l'avaloir</a>

    <a href="{{ path('commentaire_edit', { 'id': entity.id }) }}" class="btn btn-primary" role="button"><i
                class="ti ti-pencil"></i>
        Editer</a>

    <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
        <i class="ti ti-trash"></i> Supprimer
    </button>
    <br/>
{% endif %}
