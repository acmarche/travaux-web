{% if is_granted('ROLE_TRAVAUX_AVALOIR') %}

    <a href="{{ path('avaloir') }}" class="btn btn-primary" role="button"><i
                class="ti ti-arrow-left"></i>
        Retour à la liste</a>

    <a href="{{ path('datenettoyage_new', { 'id': avaloir.id }) }}" class="btn btn-primary" role="button"><i
                class="ti ti-calendar"></i>
        Ajouter une date de nettoyage</a>

    <a href="{{ path('commentaire_new', { 'id': avaloir.id }) }}" class="btn btn-primary" role="button">
        <i class="ti ti-message"></i>
        Ajouter un commentaire</a>

    <a href="{{ path('avaloir_edit', { 'id': avaloir.id }) }}" class="btn btn-primary" role="button"><i
                class="ti ti-pencil"></i>
        Editer</a>

    <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
        <i class="ti ti-trash"></i> Supprimer l'avaloir
    </button>
    <br/>
{% endif %}
