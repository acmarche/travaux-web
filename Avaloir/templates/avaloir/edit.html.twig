{% extends '@AcMarcheAvaloir/layout_avaloir.html.twig' %}

{% block body -%}
    <div class="card">
        <h3 class="card-header">Edition de l'avaloir {{ avaloir.id }} {{ avaloir.rue }}</h3>
        <div class="card-body">
            {{ form(edit_form) }}
        </div>
    </div>
{% endblock %}
