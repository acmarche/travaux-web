<a href="{{ path('employe') }}" class="btn btn-primary" role="button"><i
            class="ti ti-arrow-left"></i> Retour à la liste</a>

<a href="{{ path('absence_new', { 'id': employe.id }) }}" class="btn btn-primary"
   role="button"><i class="ti ti-plus"></i> Ajouter une absence</a>

<a href="{{ path('employe_edit', { 'id': employe.id }) }}" class="btn btn-primary"
   role="button"><i class="ti ti-pencil"></i> Editer</a>

<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
    <i class="ti ti-trash"></i> Supprimer l'ouvrier
</button>

<br/>
<br/>