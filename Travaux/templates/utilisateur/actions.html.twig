<a href="{{ path('actravaux_utilisateur_edit', { 'id': utilisateur.id }) }}" role="button"
   class="btn btn-primary"><i
        class="ti ti-pencil"></i> Rôle et notification</a>

<a href="{{ path('homepage', { 'username': utilisateur.username, '_switch_user': utilisateur.username }) }}" role="button"
   class="btn btn-primary"><i class="ti ti-user-bolt"></i> Voir en tant que</a>

<a href="{{ path('actravaux_utilisateur') }}" class="btn btn-primary" role='button'><i class="ti ti-list"></i>
    Retour à la Liste</a>

<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
    <i class="ti ti-trash"></i> Supprimer
</button>

<br/><br/>
