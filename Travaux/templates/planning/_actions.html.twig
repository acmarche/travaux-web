<div class="d-print-none">
    <a href="{{ path('planning_index', {'yearmonth': yearMonth, 'categoryPlanning': categorySelected}) }}"
       class="btn btn-primary" role="button"><i
                class="ti ti-arrow-left"></i> Retour à la liste</a>

    <a href="{{ path('planning_edit', { 'id': intervention.id,'yearmonth': yearMonth, 'categoryId': categorySelected }) }}"
       class="btn btn-primary"
       role="button"><i class="ti ti-pencil"></i> Editer</a>

    <button type="button" class="btn btn-primary" onclick="print()"
            role="button"><i class="ti ti-printer"></i> Imprimer
    </button>

    <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
        <i class="ti ti-trash"></i> Supprimer l'intervention
    </button>

    <br/>
    <br/>
</div>