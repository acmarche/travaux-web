{% set action = app.request.attributes.get("_route") %}
{% if is_granted('ROLE_TRAVAUX') %}
    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li>
            <a href="{{ path('intervention') }}" class="nav-link px-2 link-dark"><i class="fa fa-search"></i>
                Rechercher</a>
        </li>
        <li>
            <a href="{{ path('intervention_archive') }}" class="nav-link px-2 link-dark"><i
                        class="fa fa-list"></i> Archive</a>
        </li>
        {% if is_granted('ROLE_TRAVAUX_AVALOIR') %}
            <li>
                <a href="{{ path('avaloir') }}" class="nav-link px-2 link-dark"><i
                            class="fa fa-list"></i> Avaloirs</a>
            </li>
            <li>
                <a href="{{ path('stock_home') }}" class="nav-link px-2 link-dark"><i
                            class="fa fa-list"></i> Stocks</a>
            </li>
        {% endif %}
    </ul>
{% endif %}
<ul class="navbar-nav mb-2 mb-lg-0">
    {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
        <li class="nav-item">
            <a href="{{ path('app_login') }}" class="nav-link"><i class="fas fa-sign-in-alt"></i>
                Se connecter</a>
        </li>
    {% else %}
        <li class="nav-item"><a href="{{ path('app_logout') }}" class="nav-link"><i class="fas fa-sign-out-alt"></i>
                Déconnexion</a>
        </li>
    {% endif %}
</ul>
