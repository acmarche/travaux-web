{% extends '@AcMarcheStock/layout.html.twig' %}

{% block body %}
    <div class="card">
        <h3 class="card-header">Nouveau produit</h3>
        <div class="card-body">

            {{ include('@AcMarcheStock/produit/_form.html.twig') }}

            <br/>
            <a href="{{ path('stock_produit_index') }}"><i class="ti ti-arrow-left"></i>
                Annuler</a>
        </div>
    </div>
{% endblock %}
