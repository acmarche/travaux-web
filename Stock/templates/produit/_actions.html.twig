<a href="{{ path('stock_quantite_update', { 'id': produit.id }) }}" role="button"
   class="btn btn-success"><i class="fas fa-sync"></i> Mettre à jour les quantités</a>

<a href="{{ path('stock_produit_edit', { 'id': produit.id }) }}" role="button"
   class="btn btn-primary"><i class="far fa-edit"></i> Modifier</a>

<a href="{{ path('stock_produit_index') }}" class="btn btn-primary" role='button'><i class="fas fa-list"></i>
    Retour à la liste
</a>

<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
    <i class="fas fa-trash"></i> Supprimer
</button>
<br/>
<br/>