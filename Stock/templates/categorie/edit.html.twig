{% extends '@AcMarcheStock/layout.html.twig' %}

{% block title %}Edit Categorie{% endblock %}

{% block body %}
    <div class="card">
        <h3 class="card-header">{{ categorie.nom }}</h3>
        <div class="card-body">

            {{ include('@AcMarcheStock/categorie/_form.html.twig', {'button_label': 'Update'}) }}

            <br/>
            <a href="{{ path('stock_categorie_show',{'id': categorie.id}) }}"><i class="ti ti-arrow-left"></i>
                Annuler</a>
        </div>
    </div>

    {{ include('@AcMarcheStock/categorie/_delete_form.html.twig') }}
{% endblock %}
